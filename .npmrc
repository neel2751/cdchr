# next-auth declares an OPTIONAL peer on nodemailer@^7, but this app is on
# nodemailer@9 and supplies its own transport (next-auth's built-in email
# provider is not used). Without this, `npm install` fails with ERESOLVE on that
# advisory-only optional peer. Safe to keep for this project.
legacy-peer-deps=true
